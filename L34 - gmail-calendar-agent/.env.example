# Google API Configuration
# Place your credentials.json in the project root directory

# OpenAI Configuration (optional - for LLM mode)
OPENAI_API_KEY=your_openai_api_key_here

# Application Configuration
RUN_MODE=once  # Options: once, continuous
SCAN_INTERVAL=10  # Seconds between scans (only for continuous mode)

# Search Configuration (Config Mode)
SEARCH_MODE=config  # Options: config, llm
SEARCH_QUERY=is:unread subject:meeting
# Query examples:
# - is:unread from:boss@company.com
# - is:unread label:important
# - subject:appointment is:unread
# - from:calendar@example.com

# LLM Search Configuration (LLM Mode)
LLM_SEARCH_PROMPT=Find emails about meetings or appointments in the next week
# Free-text prompt for AI to interpret and create search query

# Calendar Configuration
CALENDAR_ID=primary  # Use 'primary' for main calendar or specific calendar ID
